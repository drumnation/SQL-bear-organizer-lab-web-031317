CREATE TABLE bears (
  id INTEGER PRIMARY KEY,
  name TEXT,
  age INTEGER,
  gender BIT, -- BIT is for 2 options, CHAR(1) for more than 2 possibly values
  color TEXT,
  temperament TEXT,
  alive BIT
)
